import Content from "../classes/Content"

interface IProps {
    language: languages
}

enum languages {
	ENG = "ENG",
	JPN = "JPN"
}

export default function ({language}: IProps) {
    
    return (

)}